---
name: firestore-collection
description: Add a new Firestore collection with typed service methods
---
Create a Firestore service for the "$ARGUMENTS" collection:
1. Define a TypeScript interface in types/$ARGUMENTS.ts
2. Create services/$ARGUMENTS.ts with getAll, getById, create, update, delete
3. Add proper error handling